@using AspAPI.Models.Materials
@model AspAPI.Models.Course

@{
    ViewData["Title"] = "CreateCourseForm";
}

<h1>Adding new course</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-controller="Course" asp-action="CreateCourse">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                @Html.LabelFor(x => x.Name)
                @Html.TextBoxFor(x => x.Name, new { @class = "form-control"})
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                @Html.LabelFor(x => x.Description)
                @Html.TextBoxFor(x => x.Description, new { @class = "form-control"})
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>
            @*<div class="form-group">
                @Html.LabelFor(x => x.CourseMaterials)
                <table class="table table-striped">
                    <thead>
                    <tr>
                        <th>Type</th>
                        <th>Title</th>
                    </tr>
                    </thead>
                    <tbody>
                    </tbody>
                </table>
            </div>*@
            <div class="form-group">
                <input type="submit" class="btn btn-outline-primary" value="CreateForm Course" />
            </div>
        </form>
        @*<form asp-controller="Material" asp-action="CreateForm">
            <label for="option">Material</label>
            <select class="dropdown" name="option" id="option">
                <option class="dropdown-item" value="Article">Article</option>
                <option class="dropdown-item" value="Publication">Publication</option>
                <option class="dropdown-item" value="Video">Video</option>
            </select>
            <input type="hidden" name="Name" value="dsadqweda" />
            <input type="hidden" name="Description" value="sdwdfgsw" />
            <input type="submit" class="btn btn-outline-primary" value="Add materials" />
        </form>*@
    </div>
</div>

<div>
    <a asp-controller="Identity" asp-action="UserProfile">Back</a>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
